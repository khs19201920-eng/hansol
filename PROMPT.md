# 컨쿼 법인카드 영수증 매칭 에이전트 — 개발 요구사항 프롬프트

> 이 문서는 "컨쿼(Conquer) 법인카드 영수증 매칭 에이전트"를 만들기 위한 **완성형 지시 프롬프트**입니다.
> AI 코딩 도구나 개발자에게 그대로 전달하면 동일한 결과물을 재현할 수 있습니다.
> 실제 동작하는 구현체는 같은 폴더의 `index.html` 입니다.

---

## 1. 목적 (What & Why)

법인카드 사용 후 **컨쿼(Conquer)** 시스템에 영수증을 첨부하는 업무를 자동화 보조한다.

현재 업무 흐름의 병목:

1. 컨쿼에서 법인카드 **내역 리포트(PDF)** 를 내려받는다.
2. 흩어져 있는 **영수증 사진들**을 하나하나 어떤 결제 건인지 눈으로 대조한다.
3. 컨쿼에 접속해 결제 건마다 맞는 영수증을 첨부한다.

이 에이전트는 위 2번(대조)을 자동화하고, 첨부 단계를 사람이 **파일명만 보고** 처리할 수 있도록
영수증 파일명을 규칙에 맞게 바꿔 내려받게 한다.

**최종 첨부 행위는 사람이 컨쿼에서 직접 수행한다.** 에이전트는 "정리된 파일명을 가진 영수증 묶음"을
만들어주는 것까지가 역할이다.

---

## 2. 핵심 제약 (Hard Constraints)

- **로컬 단일 HTML 파일**로 동작해야 한다. 더블클릭 → 브라우저에서 바로 실행.
- **외부로 데이터를 전송하지 않는다.** PDF 내역·영수증 이미지·추출된 금액 등 모든 민감 데이터는
  브라우저 안에서만 처리되고 절대 서버/외부 API로 업로드되지 않는다. (프라이버시 보장)
- 백엔드 서버 없음. 결제 대행/OCR 클라우드 API 없음.
- 라이브러리(PDF 파서, OCR 엔진)는 코드 실행을 위해 CDN에서 로드할 수 있으나,
  **사용자 데이터는 어떤 라이브러리에도 네트워크로 전송되지 않아야 한다.**
  (Tesseract.js·pdf.js는 전부 클라이언트에서 로컬 연산으로만 동작한다.)
- 인터넷이 없는 환경이면 OCR 자동인식은 비활성화되고, PDF 파싱과 수동입력·파일명 변환은 계속 동작해야 한다.

---

## 3. 사용자 시나리오 (User Flow)

1. **PDF 드롭**: 컨쿼 법인카드 내역 리포트(PDF)를 드래그앤드랍한다.
   → 결제내역이 표로 추출된다. (번호 / 결제일 / 금액 / 가맹점)
   → 자동 추출이 완벽하지 않을 수 있으므로 표는 **직접 수정·행 추가/삭제** 가능해야 한다.
2. **영수증 드롭**: 영수증 사진들(여러 장 한꺼번에 가능)을 드래그앤드랍한다.
   → 각 영수증에서 **금액·날짜를 OCR로 1차 자동 추출**한다. (Tesseract.js, 로컬)
   → 자동 추출값은 각 영수증 카드에서 **사람이 검수·수정**할 수 있다. (혼합 방식)
3. **자동 매칭**: 영수증의 (금액 + 날짜)를 결제내역의 (금액 + 날짜)와 대조해 자동 매칭한다.
4. **검수**: 매칭 결과를 확인한다. 미매칭/중복/모호(후보 여럿) 건은 시각적으로 구분되고
   드롭다운으로 사람이 수동 매칭을 지정할 수 있다.
5. **다운로드**: 매칭된 영수증들을 **규칙에 맞게 파일명을 바꿔** 내려받는다.
   → 개별 다운로드 및 전체 ZIP 일괄 다운로드 지원.
6. 사람이 컨쿼에 접속해, 바뀐 파일명만 보고 각 결제 건에 영수증을 첨부한다.

---

## 4. 매칭 규칙 (Matching Logic)

- **매칭 키 = 결제금액 + 결제일(yymmdd)**. 둘 다 일치해야 매칭 성립.
- 금액은 숫자만 비교(콤마·₩·원·공백 제거). 날짜는 `yymmdd` 정규화 후 비교.
- 결과 상태 구분:
  - `matched` (매칭됨): 정확히 1건의 결제내역과 일치.
  - `ambiguous` (모호): 같은 (금액+날짜) 결제내역이 2건 이상 → 사람이 드롭다운으로 선택.
  - `unmatched` (미매칭): 일치하는 결제내역 없음 → 금액/날짜 수정 후 재매칭 또는 수동 지정.
- **한 결제내역에는 하나의 영수증만** 연결. 이미 다른 영수증이 점유한 결제내역에 중복 지정되면 경고 표시.

---

## 5. 파일명 규칙 (Rename Rule)

형식: **`번호순서_yymmdd_결제금액.확장자`**

- `번호순서`: **PDF 내역에 적힌 번호를 그대로** 사용한다. (재부여하지 않음)
- `yymmdd`: 매칭된 **결제내역의 결제일**. 예) 2026-07-03 → `260703`
- `결제금액`: 매칭된 결제내역의 금액, **숫자만**(콤마 없이). 예) `13500`
- 확장자: 원본 영수증 이미지의 확장자 유지(jpg/png/heic 등).
- 파일명의 진실 원천(source of truth)은 **결제내역**이다(영수증 OCR값이 아니라).
  사람이 컨쿼의 결제 건과 파일명을 맞춰 첨부하기 때문이다.
- 예시: 내역 3번, 2026-07-03, 13,500원 → `3_260703_13500.jpg`

---

## 6. PDF 파싱 요구사항 (중요)

- pdf.js로 텍스트를 추출하고, y좌표 기준으로 줄을 재구성한 뒤, 각 줄에서
  **번호 / 날짜 / 금액 / 가맹점**을 정규식으로 best-effort 추출한다.
- 컨쿼 리포트의 정확한 표 레이아웃은 확정되지 않았으므로:
  - 다양한 날짜 포맷(`yyyy-mm-dd`, `yyyy.mm.dd`, `yy.mm.dd`, `mm/dd` 등)과
    금액 포맷(콤마, `원`, `₩`)을 허용한다.
  - 추출된 **원본 텍스트(raw)** 를 접이식으로 볼 수 있게 해, 사람이 오파싱을 직접 교정하게 한다.
  - 결과 표는 완전 편집 가능(셀 수정 + 행 추가/삭제)해야 한다.
- 실제 컨쿼 PDF 샘플이 확보되면, 그 컬럼 순서에 맞춘 전용 파서를 추가해 자동추출 정확도를 높인다.
  (초기 버전은 범용 휴리스틱 파서 + 사람 교정으로 동작한다.)

---

## 7. UI 요구사항

- 한국어 UI. 3단 흐름: ① PDF 내역 → ② 영수증 → ③ 다운로드.
- 드래그앤드랍 영역은 드롭 시 시각 피드백. 파일선택 버튼도 병행 제공.
- 결제내역 표: 번호/결제일/금액/가맹점/매칭된 영수증 컬럼. 편집 가능.
- 영수증 카드: 썸네일 + OCR 금액/날짜(편집 가능) + 매칭 상태 배지 + 결제내역 선택 드롭다운 + 바뀔 파일명 미리보기.
- 상태 색상: 매칭=초록, 모호=노랑, 미매칭=회색/빨강.
- 상단에 "모든 데이터는 이 브라우저에서만 처리되며 외부로 전송되지 않습니다" 안내문.
- 진행 표시(OCR 처리 중 스피너/진행률).

---

## 8. 다운로드 요구사항

- 매칭된 영수증만 대상으로 개별 다운로드(`a[download]`, 올바른 파일명) 지원.
- **전체 ZIP 일괄 다운로드**(JSZip, 로컬 생성) 지원. ZIP도 클라이언트에서만 생성.
- 미매칭 영수증은 다운로드 목록에서 제외하거나 별도 표시.

---

## 9. 산출물

- 의존 라이브러리를 CDN으로 로드하는 **단일 `index.html`**. 더블클릭으로 즉시 실행.
- 코드는 읽기 쉽게 구조화(파싱/OCR/매칭/렌더/다운로드 모듈 구분), 주석은 한국어.

---

## 10. 향후 개선 여지 (Nice-to-have)

- 컨쿼 실제 PDF 샘플 기반 전용 파서.
- HEIC 이미지 브라우저 디코딩 지원.
- 가맹점명 유사도까지 활용한 3중 매칭 옵션.
- 매칭 결과 CSV 내보내기(감사 로그용).
- 라이브러리까지 로컬 동봉한 완전 오프라인 패키지 버전.
